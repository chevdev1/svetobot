# 🤖 SVETOBOT - Телеграм бот для мониторинга света

Простой телеграм-бот для отслеживания графика отключений света и уведомлений в группе.

## 🚀 Быстрый старт

### 1. Подготовка окружения

Убедитесь, что у вас установлен Python 3.8+

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/SVETOBOT
cd SVETOBOT

# Создайте виртуальное окружение
python -m venv .venv

# Активируйте окружение
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Создание Телеграм бота

1. Найдите [@BotFather](https://t.me/botfather) в Телеграм
2. Отправьте `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните токен бота

### 3. Настройка бота

1. Скопируйте `config_template.py` в `config.py`:
```bash
copy config_template.py config.py
```

2. Откройте `config.py` и заполните:
   - `BOT_TOKEN` - токен от BotFather
   - `CHAT_ID` - ID вашей группы
   - `SITE_URL` - URL сайта с графиком отключений

### 4. Получение ID группы

1. Добавьте бота в вашу группу
2. Сделайте бота администратором группы
3. Отправьте любое сообщение в группу
4. Перейдите по ссылке: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
5. Найдите `chat.id` в ответе (число со знаком минус)
6. Вставьте это число в `CHAT_ID` в конфиге

### 5. Настройка парсера сайта

**ВАЖНО**: Вам нужно адаптировать функцию парсинга под конкретный сайт.

В файле `svetobot.py` найдите функцию `parse_power_schedule()` и измените её под структуру вашего сайта.

### 6. Запуск бота

```bash
python svetobot.py
```

## 📋 Команды бота

- `/start` - приветствие и справка
- `/свет` - проверить текущий статус света  
- `/help` - показать справку

## 🔄 Автоматические уведомления

Бот автоматически проверяет статус каждые 30 минут и отправляет уведомления при изменении статуса света.

## 🌐 Деплой на Netlify

### Подготовка для деплоя:

1. Создайте файл `netlify.toml`:

```toml
[build]
  publish = "."
  command = "pip install -r requirements.txt"

[build.environment]
  PYTHON_VERSION = "3.9"

[[plugins]]
  package = "netlify-plugin-python"
```

2. Создайте файл `runtime.txt`:
```
python-3.9
```

3. Добавьте переменные окружения в Netlify:
   - `BOT_TOKEN`
   - `CHAT_ID` 
   - `SITE_URL`

## 🛠️ Разработка

### Структура проекта:
```
SVETOBOT/
├── svetobot.py          # Основной файл бота
├── config_template.py   # Шаблон конфигурации
├── requirements.txt     # Зависимости Python
├── README.md           # Документация
└── .gitignore          # Игнорируемые файлы
```

### Добавление новых функций:

1. **Новая команда**: Добавьте обработчик в функцию `main()`
2. **Новый парсер**: Модифицируйте класс `PowerMonitor`
3. **Уведомления**: Используйте функцию `send_notification()`

## 🐛 Отладка

1. Проверьте логи в консоли
2. Убедитесь в правильности токена и ID группы
3. Проверьте доступность сайта для парсинга
4. Убедитесь, что бот добавлен в группу как администратор

## 📝 Примечания

- Бот работает в режиме polling (постоянные запросы к Телеграм API)
- Для production рекомендуется использовать webhook
- Не забудьте добавить `config.py` в `.gitignore`

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения
4. Создайте Pull Request